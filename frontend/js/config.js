/**
 * Configuración global del sistema Fila Virtual
 * Este archivo centraliza los parámetros que antes se configuraban por vista o URL.
 */
window.APP_CONFIG = {
    // URL base de la API de AWS (SAM)
    API_URL: 'https://c07tv3p9hf.execute-api.us-east-1.amazonaws.com/Prod'
};
